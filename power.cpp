#include <iostream>
#include <cmath>


double power (double bottom, double exponoent)
{
    double bottom = 0;
    double exponoent = 0;
    power = (bottom )
    result = power (bottom, exponoent);
}
int main()
{
    std::cout << "Enter the base integer: ";
    std::cin >> bottom;
    std::cout >> "\n Enter exponoent power: ";
    std::cin >> exponoent;
    std:: cout << "\n" result;
    return 0;
